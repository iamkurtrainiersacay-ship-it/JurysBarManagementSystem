<Window x:Class="JurysBarManagementSystem.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="JURYS Bar Login"
        Height="400"
        Width="500"
        WindowStartupLocation="CenterScreen"
        Background="White">

    <Grid>
        <Border Width="320"
                Padding="30"
                Background="White"
                CornerRadius="15"
                BorderBrush="#1E90FF"
                BorderThickness="2"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">

            <StackPanel>
                <TextBlock Text="JURYS BAR SYSTEM"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="#1E90FF"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,25"/>

                <TextBox x:Name="UsernameBox"
                         Height="35"
                         Margin="0,8"
                         Padding="8"/>

                <PasswordBox x:Name="PasswordBox"
                             Height="35"
                             Margin="0,8"
                             Padding="8"/>

                <Button Content="LOGIN"
                        Height="40"
                        Margin="0,20,0,0"
                        Background="#1E90FF"
                        Foreground="White"
                        Click="Login_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
<UserControl
    x:Class="JurysBarManagementSystem.Views.TncView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="White" Margin="20">

        <StackPanel>

            <TextBlock Text="Terms and Conditions"
                       FontSize="22"
                       Foreground="#1E90FF"
                       FontWeight="Bold"
                       Margin="0,0,0,15"/>

            <TextBox x:Name="TncBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Height="350"
                     FontSize="14"/>

        </StackPanel>

    </Grid>

</UserControl>